const
	transfer = require('./transfer'),
    history = require('./history'),
    autoTransfer = require('./autoTransfer');

module.exports = {
	transfer : transfer,
    history : history,
    autoTransfer : autoTransfer,
};