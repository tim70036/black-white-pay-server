const
    store       = require('./store'),
    agent           = require('./agent'),
    member          = require('./member'),
    user        = require('./user'),
    verify      = require('./verify');

module.exports = {
    store       : store,
    agent           : agent,
    member          : member,
    user            : user,
    verify          : verify,
};